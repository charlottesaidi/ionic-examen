import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.csaidi.artspot',
  appName: 'ArtSpot',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
